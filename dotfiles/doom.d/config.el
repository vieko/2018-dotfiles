;;; ~/.doom.d/config.el -*- lexical-binding: t; -*-

(setq doom-theme 'gruvbox-dark-medium)

(setq user-email-address "vieko.franetovic@gmail.com"
      user-full-name "Vieko Franetovic")

(setq doom-font (font-spec :family "MesloLGLDZ Nerd Font Mono" :size 26))
